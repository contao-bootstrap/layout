{% extends "@Contao/content_element/text.html.twig" %}
{% if contao_bootstrap_enabled() and image %}
  {% set img_attributes = attrs(img_attributes|default).addClass(contao_bootstrap_extract_img_classes()) %}
{% endif %}
